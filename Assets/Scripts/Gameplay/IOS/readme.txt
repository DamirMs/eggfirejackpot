errors fixing:
* add windowTypeEnums: 
        MenuShop,
        MenuTutorial,
        MenuPrivacyPolicy,
* remove wrapper sceneContext in menu scene

steps: 
* add installer to project context, add config, assign shop skins, add ShopManager to GameObject
* add additional menu buttons to menu window
* add menu bootstrapper to menu scene
* remove privacy policy script from button
* add in ScoreManager : 
_currencyManager.Add(1);

* remove wrapper, cleanup
